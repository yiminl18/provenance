#measure if summary contains ``sentence'' info, return 0(no) or 1(yes), and also proof (the closest sentences in summary)
def summary_evaluator(summary, sentence):
    a=0


if __name__ == "__main__":
    summary = ''
    sentence = ''
    result, proof = summary_evaluator(summary, sentence)
    print(result, proof)